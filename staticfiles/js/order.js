const user = sessionStorage.idd;

$(document).ready(function () {

    // // remember user
    // $('#userEdit').val(localStorage.usernameHolder);
    // $('#passEdit').val(localStorage.passwordHolder);
    

 
});